import UserInfo from "@/components/UserInfo";
import React from "react";

const page = () => {
  return (
    <div className="flex min-h-screen flex-col items-center justify-center p-10 font-mono">
      <UserInfo />
    </div>
  );
};

export default page;
